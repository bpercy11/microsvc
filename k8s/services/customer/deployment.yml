apiVersion: apps/v1
kind: Deployment
metadata:
  name: customer
  labels:
    app: customer
spec:
  replicas: 1
  template:
    metadata:
      name: customer
      labels:
        app: customer
    spec:
      containers:
        - name: customer
          image: bp111/customer:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8080
          env:
            - name: SPRING_PROFILES_ACTIVE
              value: kube
            - name: SPRING_DATASOURCE_USERNAME
              value: linpostgres
            - name: SPRING_DATASOURCE_PASSWORD
              value: vdHzDJW4AP!F6BA7
            - name: SPRING_DATASOURCE_URL
              value: jdbc:postgresql://lin-13337-3975-pgsql-primary.servers.linodedb.net:5432/customer
      restartPolicy: Always
  selector:
    matchLabels:
      app: customer